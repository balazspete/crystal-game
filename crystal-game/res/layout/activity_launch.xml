<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="top"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".CrystalGameLaunchActivity" >

    <Button
        android:id="@+id/button_create_group"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:onClick="createGroup"
        android:text="@string/button_create_group"
        android:textSize="@dimen/launch_screen_text_size" />

    <Button
        android:id="@+id/button_join_group"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/button_create_group"
        android:layout_alignLeft="@+id/button_create_group"
        android:layout_alignRight="@+id/button_create_group"
        android:onClick="joinGroup"
        android:text="@string/button_join_group"
        android:textSize="@dimen/launch_screen_text_size" />

</RelativeLayout>
